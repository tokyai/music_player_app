<?xml version="1.0" encoding="utf-8"?>
<!-- 车机迷你窗：跨 App 置顶显示当前歌曲信息 -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/fc_root"
    android:layout_width="300dp"
    android:layout_height="wrap_content"
    android:minHeight="76dp"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:background="@drawable/fc_bg"
    android:paddingStart="12dp"
    android:paddingEnd="6dp"
    android:paddingTop="10dp"
    android:paddingBottom="10dp">

    <!-- 封面 -->
    <ImageView
        android:id="@+id/fc_cover"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:scaleType="centerCrop"
        android:background="@drawable/fc_cover_bg" />

    <!-- 文字信息 -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:layout_marginStart="10dp"
        android:layout_marginEnd="6dp">

        <TextView
            android:id="@+id/fc_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="歌曲名"
            android:textColor="#FFFFFF"
            android:textSize="16sp"
            android:textStyle="bold"
            android:maxLines="1"
            android:ellipsize="end" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/fc_artist"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="歌手"
                android:textColor="#B3FFFFFF"
                android:textSize="12sp"
                android:maxLines="1"
                android:ellipsize="end" />

            <TextView
                android:id="@+id/fc_status"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="6dp"
                android:text="播放中"
                android:textColor="#80C8FF"
                android:textSize="11sp"
                android:maxLines="1" />
        </LinearLayout>
    </LinearLayout>

    <!-- 播放/暂停 -->
    <ImageButton
        android:id="@+id/fc_play"
        android:layout_width="52dp"
        android:layout_height="52dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:src="@drawable/ic_play_white"
        android:padding="10dp"
        android:contentDescription="播放或暂停" />

    <!-- 隐藏迷你窗，不影响当前歌曲播放 -->
    <ImageButton
        android:id="@+id/fc_hide"
        android:layout_width="42dp"
        android:layout_height="42dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:src="@drawable/ic_close_white"
        android:padding="11dp"
        android:contentDescription="隐藏迷你窗" />

</LinearLayout>
